// Single place the SDK is imported from, so that the whole repo shares one instance of it.
// `links` in deno.json points this at the sibling checkout during development.
export * as mawa from 'mawa'
